# 🍊💛 TIL — Introdução ao Git

**Data:** <!-- coloque aqui a data -->  
**Mentor(a):** <!-- nome do(a) mentor(a) -->  
**Programa de Mentoria:** 1º Edição 

---

## 💡 O que aprendi

Hoje aprendi os **conceitos básicos do Git** — o que é, pra que serve e como usar os principais comandos no dia a dia.

- Git é um **sistema de controle de versão** que registra o histórico das mudanças do código.  
- Ele permite **trabalhar em equipe**, **voltar no tempo** e **acompanhar a evolução do projeto**.  
- Cada versão registrada se chama **commit**, e o local onde o projeto fica guardado é o **repositório**.  

---

## ⚙️ Comandos que pratiquei

```bash
git init           # cria um novo repositório
git clone          # clona um repositório existente
git status         # mostra o que mudou
git add .          # adiciona os arquivos para o commit
git commit -m "mensagem"   # registra a mudança
git log            # mostra o histórico de commits
git push           # envia para o repositório remoto (GitHub)
git pull           # traz as mudanças mais recentes
```
